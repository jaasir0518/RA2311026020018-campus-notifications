# Notification App Backend

Stage 1 lives here. The entry point is `src/stage1.ts`.

## Run

1. Build the logging middleware and backend TypeScript output.
2. Set the required evaluation environment variables.
3. Run the Stage 1 script and capture the JSON output screenshot.
